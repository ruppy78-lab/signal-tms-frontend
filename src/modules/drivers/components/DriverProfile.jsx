import React from 'react';

// DriverProfile is no longer used — editing is handled by DriverForm
// Kept for backwards compatibility if referenced elsewhere
export default function DriverProfile() { return null; }
